# Finance Research Agent - Environment Variables

# The Anthropic API key for the agent model (e.g., claude-haiku-4-5)
# Note: This is required for real agent execution, but the build and foundation tests will pass without it.
ANTHROPIC_API_KEY=""
